import { Group } from "three";
import IAreaLight from "../../interface/IAreaLight";
import ObjectManager from "../core/ObjectManager";
export default class AreaLight extends ObjectManager<Group> implements IAreaLight {
    static componentName: string;
    static defaults: import("../../interface/utils/Defaults").default<IAreaLight>;
    static schema: Required<import("../../interface/utils/extractProps").ExtractProps<IAreaLight>>;
    private light?;
    constructor();
    private helperState;
    get helper(): boolean;
    set helper(val: boolean);
    private _color?;
    get color(): string;
    set color(val: string);
    private _intensity?;
    get intensity(): number;
    set intensity(val: number);
    private _width?;
    get width(): number;
    set width(val: number);
    private _height?;
    get height(): number;
    set height(val: number);
    private _scaleX?;
    get scaleX(): number;
    set scaleX(val: number);
    private _scaleY?;
    get scaleY(): number;
    set scaleY(val: number);
    get depth(): number;
    set depth(_: number);
    get scaleZ(): number;
    set scaleZ(_: number);
}
