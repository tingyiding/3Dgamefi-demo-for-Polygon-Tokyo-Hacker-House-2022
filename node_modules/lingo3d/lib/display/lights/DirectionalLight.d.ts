import { DirectionalLight as ThreeDirectionalLight } from "three";
import IDirectionalLight from "../../interface/IDirectionalLight";
import LightBase from "../core/LightBase";
export default class DirectionalLight extends LightBase<ThreeDirectionalLight> implements IDirectionalLight {
    static componentName: string;
    static defaults: import("../../interface/utils/Defaults").default<IDirectionalLight>;
    static schema: Required<import("../../interface/utils/extractProps").ExtractProps<IDirectionalLight>>;
    constructor();
}
