import { PointLight as ThreePointLight } from "three";
import IPointLight from "../../interface/IPointLight";
import LightBase from "../core/LightBase";
import PointLightMixin from "../core/mixins/PointLightMixin";
declare class PointLight extends LightBase<ThreePointLight> implements IPointLight {
    static componentName: string;
    static defaults: import("../../interface/utils/Defaults").default<IPointLight>;
    static schema: Required<import("../../interface/utils/extractProps").ExtractProps<IPointLight>>;
    constructor();
}
interface PointLight extends LightBase<ThreePointLight>, PointLightMixin<ThreePointLight> {
}
export default PointLight;
