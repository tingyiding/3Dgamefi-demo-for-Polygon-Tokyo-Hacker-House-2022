import ITrigger from "../interface/ITrigger";
import PositionedItem from "../api/core/PositionedItem";
export default class Trigger extends PositionedItem implements ITrigger {
    static componentName: string;
    static defaults: import("../interface/utils/Defaults").default<ITrigger>;
    static schema: Required<import("../interface/utils/extractProps").ExtractProps<ITrigger>>;
    private refresh;
    onEnter: (() => void) | undefined;
    onExit: (() => void) | undefined;
    private _pad;
    get pad(): boolean;
    set pad(val: boolean);
    private _radius;
    get radius(): number;
    set radius(val: number);
    private _interval;
    get interval(): number;
    set interval(val: number);
    private _helper;
    get helper(): boolean;
    set helper(val: boolean);
    private _targetIds?;
    get targetIds(): string | string[] | undefined;
    set targetIds(val: string | string[] | undefined);
    constructor();
}
