import { OrthographicCamera as ThreeOrthographicCamera } from "three";
import { camFar } from "../../engine/constants";
import { ORTHOGRAPHIC_FRUSTUM } from "../../globals";
import { getResolution } from "../../states/useResolution";
import CameraBase from "../core/CameraBase";
//@ts-ignore
export default class OrthographicCamera extends CameraBase {
    constructor() {
        const [w, h] = getResolution();
        const aspect = w / h;
        super(new ThreeOrthographicCamera(aspect * ORTHOGRAPHIC_FRUSTUM * -0.5, aspect * ORTHOGRAPHIC_FRUSTUM * 0.5, ORTHOGRAPHIC_FRUSTUM * 0.5, ORTHOGRAPHIC_FRUSTUM * -0.5, -1, camFar));
    }
}
//# sourceMappingURL=OrthographicCamera.js.map