import { CubeTextureLoader } from "three";
import { forceGet } from "@lincode/utils";
import { increaseLoadingCount, decreaseLoadingCount } from "../../../states/useLoadingCount";
import { handleProgress } from "./bytesLoaded";
const cache = new Map();
const loader = new CubeTextureLoader();
export default (urls) => forceGet(cache, urls.join(","), () => {
    increaseLoadingCount();
    return loader.load(urls, decreaseLoadingCount, handleProgress, decreaseLoadingCount);
});
//# sourceMappingURL=loadCubeTexture.js.map