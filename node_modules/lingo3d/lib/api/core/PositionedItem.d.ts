import IPositioned from "../../interface/IPositioned";
import EventLoopItem from "./EventLoopItem";
export default abstract class PositionedItem extends EventLoopItem implements IPositioned {
    get x(): number;
    set x(val: number);
    get y(): number;
    set y(val: number);
    get z(): number;
    set z(val: number);
    getWorldPosition(): import("@lincode/math").Point3d;
}
export declare const isPositionedItem: (item: any) => item is PositionedItem;
