import { h } from "preact";
declare type ObjectIconProps = {
    name: string;
    iconName?: string;
};
declare const ObjectIcon: ({ name, iconName }: ObjectIconProps) => h.JSX.Element;
export default ObjectIcon;
