import { h } from "preact";
import { CSSProperties } from "react";
declare type ExpandIconProps = {
    style?: CSSProperties;
    onClick?: () => void;
};
declare const ExpandIcon: ({ style, onClick }: ExpandIconProps) => h.JSX.Element;
export default ExpandIcon;
