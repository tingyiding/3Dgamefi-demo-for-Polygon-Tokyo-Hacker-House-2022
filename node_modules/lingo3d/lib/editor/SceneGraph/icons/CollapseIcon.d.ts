import { h } from "preact";
import { CSSProperties } from "react";
declare type CollapseIconProps = {
    style?: CSSProperties;
    onClick?: () => void;
};
declare const CollapseIcon: ({ style, onClick }: CollapseIconProps) => h.JSX.Element;
export default CollapseIcon;
