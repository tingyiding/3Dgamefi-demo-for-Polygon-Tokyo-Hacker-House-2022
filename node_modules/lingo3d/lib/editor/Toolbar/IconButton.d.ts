/// <reference types="react" />
import { h } from "preact";
declare type IconButtonProps = {
    children: JSX.Element;
    onClick?: () => void;
    active?: boolean;
    disabled?: boolean;
};
declare const IconButton: ({ children, onClick, active, disabled }: IconButtonProps) => h.JSX.Element;
export default IconButton;
