import downloadBlob from "../../utils/downloadBlob";
export default (filename, data) => {
    const blob = new Blob([data], { type: "text/plain" });
    downloadBlob(filename, blob);
};
//# sourceMappingURL=saveTextFile.js.map