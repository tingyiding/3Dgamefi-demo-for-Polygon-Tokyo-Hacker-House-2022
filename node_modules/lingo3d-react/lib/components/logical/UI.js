import React from "react";
import ReactDOM from "react-dom";
import htmlContainer from "./HTML/htmlContainer";
const UI = ({ children }) => {
    return ReactDOM.createPortal(React.createElement("div", { style: { pointerEvents: "all", position: "static" } }, children), htmlContainer);
};
export default UI;
