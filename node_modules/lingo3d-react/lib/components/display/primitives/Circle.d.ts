import React from "react";
import { Circle as GameCircle } from "lingo3d";
declare const Circle: React.ForwardRefExoticComponent<Partial<import("lingo3d/lib/interface/IPrimitive").default> & {
    children?: React.ReactNode;
} & React.RefAttributes<GameCircle>>;
export default Circle;
