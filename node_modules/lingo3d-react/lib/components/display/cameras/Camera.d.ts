import React from "react";
import { Camera as GameCamera } from "lingo3d";
declare const Camera: React.ForwardRefExoticComponent<Partial<import("lingo3d/lib/interface/ICamera").default> & {
    children?: React.ReactNode;
} & React.RefAttributes<GameCamera>>;
export default Camera;
